#include "main.h"


void puts2(char *str) 
{
	while (*str != '\0' && *(str++) != '\0')
        {
		s--;
                _putchar(*str);
                str++;
		str++;
        }
        _putchar('\n');
}
